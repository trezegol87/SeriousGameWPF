<Page x:Class="SeriousGameWPF.EndScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:gif="http://wpfanimatedgif.codeplex.com"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="EndScreen">

    <Grid>
        <Viewbox>
            <Canvas x:Name="PlayArea" Background="{Binding Background}" Width="800" Height="600">
                <!-- <Image gif:ImageBehavior.AnimatedSource="Images/436627.gif" HorizontalAlignment="Left" Height="100" Margin="103,142,0,0" VerticalAlignment="Top" Width="100"/>-->
            </Canvas>
        </Viewbox>

    </Grid>
</Page>
